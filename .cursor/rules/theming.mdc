---
description: These theming guidelines are crucial when implementing UI
globs: 
---
## Use Theme for Styling

```typescript
// ❌ Bad: Hardcoded styles
function ProfileCard() {
  return (
    <View style={{
      backgroundColor: '#f5f5f5',
      padding: 16,
      marginBottom: 8
    }}>
      <Text style={{ color: '#333' }}>Profile</Text>
    </View>
  );
}

// ✅ Good: Using theme
function ProfileCard() {
  const { theme } = useAppTheme();
  return (
    <View style={{
      backgroundColor: theme.colors.background,
      padding: theme.spacing.medium,
      marginBottom: theme.spacing.small
    }}>
      <Text style={{ color: theme.colors.text }}>Profile</Text>
    </View>
  );
}
```

## Use Design System Components

```typescript
// ❌ Bad: Custom styled components
function Button({ children }) {
  return (
    <TouchableOpacity style={{
      backgroundColor: 'blue',
      padding: 10,
      borderRadius: 5
    }}>
      <Text style={{ color: 'white' }}>{children}</Text>
    </TouchableOpacity>
  );
}

// ✅ Good: Using design system
import { Button } from '@/design-system';

function SubmitButton({ children }) {
  return <Button variant="primary">{children}</Button>;
}
```
